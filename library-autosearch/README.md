# library-autosearch

### Usage

1. Hit "export library" on https://www.goodreads.com/review/import and place the `goodreads_library_export.csv` in working directory
2. Generate sqlite DB from CSV with `import.sh`
3. Run `main.py`

### Future Ideas

- Search ISBN first before normalized title/author
